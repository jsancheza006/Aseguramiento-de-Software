import { Shield } from 'lucide-react'

export default function LogoIcon() {
  return (
    <div className="flex items-center justify-center w-8 h-8 rounded-md bg-[var(--primary)] text-[var(--primary-fg)] shrink-0">
      <Shield size={18} />
    </div>
  )
}
